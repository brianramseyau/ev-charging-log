node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dev-dist

# OS
.DS_Store
Thumbs.db

# Local agent/tooling state
.claude/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# electron-builder output
/release

# Personal data — this repo is intended to go public eventually.
# Never commit real spreadsheets, exports, or the live database:
# they contain name, vehicle rego, addresses, and charging history.
*.xlsx
*.xls
*.csv
*.sqlite
*.sqlite3
*.db
data/
# Exception: the sanitized, synthetic export template (placeholder data only,
# generated by scripts/generate-template.mjs) is meant to be checked in.
!static/templates/*.xlsx

# Generated assets — rebuilt from src/theme and src/lib/assets via `npm run assets:generate`
static/smui/
static/icons/
electron/resources/
